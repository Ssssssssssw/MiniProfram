var a = 1;
var name = "sun";
var address = function(str1,str2){
  return str1 + str2
}
function test(){
  return "sunshibo"
}

module.exports = {
  a : a,
  name : name,
  address : address,
  test : test
}